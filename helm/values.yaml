repoURL: "https://github.com/hfenner/ansibleforge.git"
targetRevision: "main"
bootstrapPath: "ocp/gitops/bootstrap"

argocd:
  namespace: openshift-gitops
  project: default
  destination:
    server: https://kubernetes.default.svc

syncPolicy:
  automated:
    prune: true
    selfHeal: true
  syncOptions:
    - ServerSideApply=true

# Cluster apps domain (e.g., apps.fire.sandbox1370.opentlc.com)
# Auto-detected by bootstrap workflow; passed to all child apps
clusterDomain: ""

# Additional values files to overlay on the bootstrap chart
# e.g., ["values-aws.yaml"] to enable AWS-specific components
valueFiles: []

# Cluster-specific AWS values (passed through to bootstrap chart as helm parameters)
aws:
  efs:
    filesystemId: ""
  ingressCerts:
    acmeEmail: ""
    route53Region: ""
    route53HostedZoneID: ""
