{{- $clusterDomain := .Values.clusterDomain }}
{{- if not $clusterDomain }}
{{- $ingressConfig := lookup "config.openshift.io/v1" "Ingress" "" "cluster" }}
{{- if and $ingressConfig $ingressConfig.spec }}
{{- $clusterDomain = $ingressConfig.spec.domain }}
{{- end }}
{{- end }}
apiVersion: console.openshift.io/v1
kind: ConsoleLink
metadata:
  name: keycloak-master
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  location: ApplicationMenu
  text: Keycloak (Master Realm)
  href: https://keycloak.{{ $clusterDomain }}/admin/master/console/
  applicationMenu:
    section: Red Hat Applications
    imageURL: https://keycloak.{{ $clusterDomain }}/favicon.ico
---
apiVersion: console.openshift.io/v1
kind: ConsoleLink
metadata:
  name: keycloak-sso
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  location: ApplicationMenu
  text: Keycloak (SSO Realm)
  href: https://keycloak.{{ $clusterDomain }}/admin/sso/console/
  applicationMenu:
    section: Red Hat Applications
    imageURL: https://keycloak.{{ $clusterDomain }}/favicon.ico
